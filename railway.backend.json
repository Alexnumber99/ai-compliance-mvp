{
  "build": {
    "dockerfile": "backend/Dockerfile",
    "context": "."
  },
  "start": {
    "cmd": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "env": {
      "PORT": 8000
    }
  }
}